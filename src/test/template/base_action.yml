name: Test GitHub Action

jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      
      - name: Run a simple command
        run: echo "This is a basic GitHub Action using a third-party action (actions/checkout)"
